# Концепция работы ссылки на игру (Приглашения друзей)

## Обзор

Система приглашений позволяет игрокам приглашать своих друзей напрямую в игру через ссылку. При переходе по ссылке игрок автоматически присоединяется к комнате без необходимости поиска игры.

## Архитектура

### Компоненты системы

1. **Бэкенд** — эндпоинты для создания приглашений и присоединения к комнатам
2. **Telegram Bot** — отправка сообщений с кнопкой приглашения
3. **Telegram Mini App** — фронтенд, который обрабатывает параметр `room` в URL
4. **Redis** — хранение состояния комнат
5. **Centrifugo** — уведомления о событиях в реальном времени

## Поток работы приглашения

### Шаг 1: Создание приглашения

**Инициатор:** Игрок, который хочет пригласить друга

**Процесс:**
1. Игрок находится в комнате (статус `waiting` или `matched`)
2. Игрок нажимает кнопку "Пригласить друга" в интерфейсе
3. Фронтенд показывает список друзей (полученный через `/friends/{tg_id}`)
4. Игрок выбирает друга из списка
5. Фронтенд отправляет запрос на `/friends/invite_to_room` с:
   - `inviter_id` — ID приглашающего игрока
   - `friend_id` — ID приглашаемого друга
   - `room_id` — ID комнаты

**Валидация на бэкенде:**
- Проверка, что пользователи являются друзьями
- Проверка существования комнаты
- Проверка, что приглашающий находится в комнате
- Проверка, что комната не заполнена
- Проверка баланса друга (должен быть ≥ ставки комнаты)
- Для `reliable_only` комнат — проверка надежности друга (опционально)

### Шаг 2: Генерация ссылки

**Формат ссылки:**
```
{FRONT_URL}/?room={room_id}
```

**Пример:**
```
https://grantexpert.pro/frontend/?room=100_abc12345
```

**Где:**
- `FRONT_URL` — базовый URL фронтенда из конфигурации (`settings.FRONT_URL`)
- `room_id` — уникальный идентификатор комнаты

### Шаг 3: Отправка приглашения

**Через Telegram Bot:**
1. Бот отправляет сообщение приглашаемому другу
2. Сообщение содержит:
   - Имя приглашающего игрока
   - Информацию о комнате (ставка, количество игроков, режим)
   - Кнопку с `WebAppInfo`, указывающую на ссылку

**Формат кнопки:**
```json
{
  "text": "🎮 Присоединиться к игре",
  "web_app": {
    "url": "https://grantexpert.pro/frontend/?room=100_abc12345"
  }
}
```

**Текст сообщения:**
```
🎮 [Имя игрока] приглашает тебя присоединиться к игре!

💰 Ставка: 100 кристаллов
👥 Игроков: 1/2
🎯 Режим: normal

Нажми кнопку ниже, чтобы открыть игру и присоединиться:
```

### Шаг 4: Получение приглашения

**Получатель:** Приглашаемый друг

**Процесс:**
1. Друг получает сообщение от бота в Telegram
2. Друг нажимает кнопку "🎮 Присоединиться к игре"
3. Telegram открывает Mini App с URL: `{FRONT_URL}/?room={room_id}`

### Шаг 5: Обработка на фронтенде

**При загрузке Mini App:**

1. **Проверка параметра `room` в URL:**
   - Фронтенд читает параметр `?room={room_id}` из URL
   - Если параметр присутствует → это приглашение

2. **Присоединение к комнате:**
   - Вызывается функция `handleJoinRoom(room_id)`
   - Выполняется та же логика, что и при клике на комнату из списка

3. **Логика присоединения:**
   - Проверка, что пользователь еще не в комнате
   - Отправка запроса на `/burkozel/join_room` с:
     - `room_id` — ID комнаты из URL
     - `tg_id` — ID пользователя
     - `nickname` — никнейм пользователя

4. **Обновление состояния:**
   - Подписка на события комнаты (`room#{room_id}`)
   - Подписка на события пользователя (`user#{tg_id}`)
   - Загрузка состояния комнаты через `/burkozel/room/{room_id}`
   - Переход в интерфейс игры


### Шаг 6: Уведомление других игроков

**После успешного присоединения:**
1. Отправляется событие `player_joined` через Centrifugo
2. Все игроки в комнате получают уведомление о новом игроке
3. Если комната заполнена → статус меняется на `matched`
4. Если все игроки готовы → игра может начаться

## Сценарии использования

### Сценарий 1: Успешное приглашение

1. Игрок А создает комнату (или присоединяется к существующей)
2. Игрок А приглашает друга Б
3. Друг Б получает сообщение от бота
4. Друг Б нажимает кнопку в сообщении
5. Mini App открывается с параметром `?room={room_id}`
6. Фронтенд автоматически присоединяет Б к комнате
7. Б видит игру и может начать играть

### Сценарий 2: Комната заполнена

1. Игрок А приглашает друга Б
2. Тем временем в комнату присоединяется другой игрок
3. Друг Б переходит по ссылке
4. Запрос `/burkozel/join_room` возвращает ошибку "Комната уже заполнена"
5. Фронтенд показывает сообщение об ошибке
6. Б может вернуться к поиску игры

### Сценарий 3: Недостаточный баланс

1. Игрок А приглашает друга Б
2. У друга Б недостаточно средств для ставки
3. При создании приглашения проверка баланса на бэкенде обнаруживает проблему
4. Приглашение не отправляется, возвращается ошибка
5. Игрок А видит сообщение об ошибке

### Сценарий 4: Игра уже началась

1. Игрок А приглашает друга Б
2. Тем временем игра начинается (все нажали "Готов")
3. Друг Б переходит по ссылке
4. Комната в статусе `playing`
5. Запрос `/burkozel/join_room` возвращает ошибку "Игра уже началась"
6. Фронтенд показывает сообщение об ошибке

### Сценарий 5: Повторное использование ссылки

1. Друг Б успешно присоединился к комнате по ссылке
2. Б случайно закрыл Mini App
3. Б снова открывает Mini App по той же ссылке
4. Фронтенд проверяет параметр `room` в URL
5. При попытке присоединиться возвращается "Игрок уже в комнате"
6. Система автоматически восстанавливает игру через механизм возврата

## Обработка ошибок

### Типичные ошибки и их обработка

1. **"Комната не найдена"**
   - Комната была удалена или истек таймаут
   - Фронтенд показывает сообщение и предлагает поиск новой игры

2. **"Комната уже заполнена"**
   - В комнату уже присоединились другие игроки
   - Фронтенд показывает сообщение и предлагает поиск новой игры

3. **"Недостаточно средств"**
   - У друга недостаточно кристаллов для ставки
   - Ошибка обнаруживается на этапе приглашения (бэкенд)
   - Игрок А видит сообщение об ошибке

4. **"Игра уже началась"**
   - Комната перешла в статус `playing`
   - Фронтенд показывает сообщение об ошибке
   - Предложение поиска новой игры

5. **"Пользователь не в списке друзей"**
   - Ошибка на этапе приглашения (бэкенд)
   - Игрок А видит сообщение об ошибке

6. **"Эта комната только для надежных игроков"**
   - Для `reliable_only` комнат, если друг ненадежен
   - Ошибка на этапе приглашения или присоединения
   - Соответствующее сообщение пользователю

## Безопасность

### Проверки при присоединении

- Проверка существования комнаты
- Проверка, что игрок еще не в комнате
- Проверка вместимости комнаты
- Проверка баланса игрока
- Проверка надежности для `reliable_only` комнат

### Защита от злоупотреблений

- Приглашения возможны только между друзьями
- Приглашающий должен быть в комнате
- Комната должна существовать и быть доступной
- Все проверки выполняются на бэкенде

### Ограничения

- Комната удаляется через 1 час после создания (TTL в Redis)
- Приглашения действительны только пока комната в статусе `waiting` или `matched`
- Игрок не может пригласить самого себя
- Игрок не может пригласить друга, который уже в комнате

## Интеграция с системой друзей

### Получение списка друзей

**Эндпоинт:** `GET /friends/{tg_id}`

**Ответ:**
```json
{
  "tg_id": 123456789,
  "friends": [
    {
      "tg_id": 987654321,
      "name": "Имя друга",
      "username": "username_друга"
    }
  ]
}
```

**Использование:**
- Фронтенд получает список друзей
- Отображает их в интерфейсе для выбора
- При клике на друга отправляется приглашение

## Преимущества системы

1. **Удобство:**
   - Игроки могут легко приглашать друзей одним кликом
   - Автоматическое присоединение без поиска

2. **Безопасность:**
   - Приглашения только между друзьями
   - Множественные проверки на бэкенде

3. **Надежность:**
   - Обработка всех сценариев ошибок
   - Восстановление игры при повторном открытии

4. **Интеграция с Telegram:**
   - Использование встроенных возможностей Mini App
   - Кнопки Web App для прямого открытия

## Дальнейшие улучшения

### Возможные расширения

1. **QR-код приглашения:**
   - Генерация QR-кода для приглашения
   - Сканирование через Telegram

2. **Групповые приглашения:**
   - Приглашение нескольких друзей одновременно
   - Групповые комнаты

3. **Временные ссылки:**
   - Ссылки с ограниченным сроком действия
   - Одноразовые приглашения

4. **Приглашения в личных сообщениях:**
   - Отправка ссылок напрямую в чат
   - Интеграция с Telegram Inline Bot

5. **История приглашений:**
   - Сохранение истории приглашений
   - Статистика принятых/отклоненных приглашений

