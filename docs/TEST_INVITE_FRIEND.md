# Тестирование эндпоинта приглашения друга `/friends/invite_to_room`

## 📋 Что реализовано

Эндпоинт `POST /friends/invite_to_room` отправляет приглашение другу присоединиться к существующей комнате через Telegram бота с кнопкой Web App (Mini App).

### Функционал:
1. ✅ Проверка дружбы между пользователями
2. ✅ Проверка существования комнаты в Redis
3. ✅ Проверка что приглашающий в комнате
4. ✅ Проверка что комната не заполнена
5. ✅ Проверка баланса друга
6. ✅ Генерация прямой ссылки на Mini App
7. ✅ Отправка сообщения через Telegram бота с кнопкой Web App

---

## 🧪 Как протестировать без фронтенда

### Шаг 1: Подготовка данных

#### 1.1. Создать двух пользователей в БД
Используйте скрипт `scripts/create_test_users.py` или добавьте вручную через API:
- Пользователь 1 (приглашающий): `tg_id = 111111`, `balance >= 1000`
- Пользователь 2 (друг): `tg_id = 222222`, `balance >= 1000`

#### 1.2. Добавить их в друзья
```bash
curl -X POST "http://localhost:8001/friends/add" \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 111111,
    "friend_id": 222222
  }'
```

#### 1.3. Создать комнату с пользователем 1
```bash
curl -X POST "http://localhost:8001/burkozel/find_player" \
  -H "Content-Type: application/json" \
  -d '{
    "tg_id": 111111,
    "nickname": "Игрок 1",
    "stake": 1000,
    "capacity": 2,
    "speed": "normal",
    "redeal": false,
    "dark": false,
    "reliable_only": false
  }'
```

**Сохраните `room_id` из ответа!**

---

### Шаг 2: Отправка приглашения

```bash
curl -X POST "http://localhost:8001/friends/invite_to_room" \
  -H "Content-Type: application/json" \
  -d '{
    "inviter_id": 111111,
    "friend_id": 222222,
    "room_id": "1000_abc12345"
  }'
```

**Ожидаемый успешный ответ:**
```json
{
  "ok": true,
  "room_id": "1000_abc12345",
  "invite_sent": true,
  "message": "Приглашение отправлено другу",
  "game_link": "https://grantexpert.pro/frontend/?room=1000_abc12345",
  "friend_id": 222222,
  "stake": 1000,
  "players_count": 1,
  "capacity": 2
}
```

---

### Шаг 3: Проверка результатов

#### ✅ Что проверяется автоматически:

1. **Валидация данных:**
   - Проверка дружбы
   - Проверка существования комнаты
   - Проверка что приглашающий в комнате
   - Проверка что комната не заполнена
   - Проверка баланса друга

2. **Генерация ссылки:**
   - Проверьте что в ответе есть `game_link`
   - Формат: `{FRONT_URL}/?room={room_id}`
   - Значение берется из `settings.FRONT_URL`

3. **Отправка сообщения:**
   - Проверьте логи приложения:
     - `[INVITE] Invite sent successfully...` - успех
     - `[INVITE] Failed to send message...` - ошибка

#### ✅ Что нужно проверить вручную:

1. **Telegram сообщение:**
   - Откройте Telegram пользователя 222222
   - Проверьте что пришло сообщение от бота
   - Проверьте что есть кнопка "🎮 Присоединиться к игре"

2. **Работа кнопки:**
   - Нажмите на кнопку
   - Должен открыться Mini App
   - В URL должен быть параметр `?room={room_id}`

---

## 🐛 Тестирование ошибок

### 1. Пользователь не в друзьях
```bash
curl -X POST "http://localhost:8001/friends/invite_to_room" \
  -H "Content-Type: application/json" \
  -d '{
    "inviter_id": 111111,
    "friend_id": 999999,
    "room_id": "1000_abc12345"
  }'
```
**Ожидаемый ответ:** `403 - Пользователь не в списке друзей`

### 2. Комната не найдена
```bash
curl -X POST "http://localhost:8001/friends/invite_to_room" \
  -H "Content-Type: application/json" \
  -d '{
    "inviter_id": 111111,
    "friend_id": 222222,
    "room_id": "nonexistent_room"
  }'
```
**Ожидаемый ответ:** `404 - Комната не найдена`

### 3. Приглашающий не в комнате
Создайте комнату с другим пользователем, затем попробуйте пригласить:
**Ожидаемый ответ:** `403 - Вы не находитесь в этой комнате`

### 4. Комната заполнена
Сначала заполните комнату (2 игрока для capacity=2), затем попробуйте пригласить третьего:
**Ожидаемый ответ:** `400 - Комната уже заполнена`

### 5. Недостаточно средств
Уменьшите баланс пользователя 222222 до < 1000:
**Ожидаемый ответ:** `400 - У друга недостаточно средств`

### 6. Бот заблокирован
Если пользователь 222222 не запускал бота или заблокировал его:
**Ожидаемый ответ:** `500 - Не удалось отправить сообщение...`

---

## 📝 Полный скрипт тестирования (Python)

Создайте файл `test_invite.py`:

```python
import requests
import json

BASE_URL = "http://localhost:8001"

def test_invite_friend():
    print("=== Тест приглашения друга ===\n")
    
    # 1. Добавить друзей
    print("1. Добавление друзей...")
    response = requests.post(f"{BASE_URL}/friends/add", json={
        "user_id": 111111,
        "friend_id": 222222
    })
    print(f"   Результат: {response.json()}\n")
    
    # 2. Создать комнату
    print("2. Создание комнаты...")
    response = requests.post(f"{BASE_URL}/burkozel/find_player", json={
        "tg_id": 111111,
        "nickname": "Игрок 1",
        "stake": 1000,
        "capacity": 2,
        "speed": "normal",
        "redeal": False,
        "dark": False,
        "reliable_only": False
    })
    room_data = response.json()
    room_id = room_data["room_id"]
    print(f"   Комната создана: {room_id}\n")
    
    # 3. Пригласить друга
    print("3. Отправка приглашения...")
    response = requests.post(f"{BASE_URL}/friends/invite_to_room", json={
        "inviter_id": 111111,
        "friend_id": 222222,
        "room_id": room_id
    })
    
    if response.status_code == 200:
        result = response.json()
        print(f"   ✅ Успех!")
        print(f"   Комната: {result['room_id']}")
        print(f"   Ссылка: {result['game_link']}")
        print(f"   Ставка: {result['stake']}")
        print(f"   Игроков: {result['players_count']}/{result['capacity']}")
        print("\n   Проверьте Telegram пользователя 222222 - должно прийти сообщение!")
    else:
        print(f"   ❌ Ошибка: {response.status_code}")
        print(f"   {response.json()}")

if __name__ == "__main__":
    test_invite_friend()
```

Запустите:
```bash
python test_invite.py
```

---

## 🔍 Проверка без запуска бота

Если бот не запущен, можно проверить что:
1. ✅ Все валидации работают
2. ✅ Ссылка генерируется правильно
3. ❌ Сообщение не отправится (получите ошибку 500)

Для проверки только генерации ссылки:
```bash
curl -X POST "http://localhost:8001/friends/invite_to_room" \
  -H "Content-Type: application/json" \
  -d '{
    "inviter_id": 111111,
    "friend_id": 222222,
    "room_id": "test_room"
  }' 2>&1 | grep "game_link"
```

---

## 📊 Логирование

Все операции логируются. Проверьте логи приложения:

```
[INVITE] inviter_id=111111, friend_id=222222, room_id=1000_abc12345
[INVITE] Generated game link: https://grantexpert.pro/frontend/?room=1000_abc12345
[INVITE] Invite sent successfully to 222222 for room 1000_abc12345
```

Или при ошибке:
```
[INVITE] Failed to send message to 222222: bot was blocked by the user
```

---

## ✅ Чек-лист проверки

- [ ] Пользователи созданы в БД
- [ ] Пользователи добавлены в друзья
- [ ] Комната создана с пользователем 1
- [ ] У обоих пользователей достаточно баланса
- [ ] Запрос на приглашение возвращает 200
- [ ] В ответе есть правильная ссылка `game_link`
- [ ] В логах нет ошибок
- [ ] В Telegram пользователю 222222 пришло сообщение
- [ ] В сообщении есть кнопка "🎮 Присоединиться к игре"
- [ ] При нажатии на кнопку открывается Mini App

---

## 🚀 Готово!

Эндпоинт готов к использованию. Фронтенд должен:
1. Вызвать `/friends/invite_to_room` при клике на друга
2. Показать уведомление об успешной отправке
3. На стороне друга - автоматически присоединиться к комнате по параметру `?room=...` из URL Mini App

