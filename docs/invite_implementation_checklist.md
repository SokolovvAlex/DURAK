# Чеклист готовности реализации приглашений

## ✅ Готовые компоненты

### Бэкенд

- [x] **Эндпоинт `/friends/invite_to_room`** (`app/friends/router.py`)
  - Валидация дружбы
  - Проверка существования комнаты
  - Проверка присутствия приглашающего в комнате
  - Проверка заполненности комнаты
  - Проверка баланса друга
  - Генерация ссылки на Mini App
  - Отправка сообщения через Telegram Bot

- [x] **Эндпоинт `/burkozel/join_room`** (`app/game/api/router.py`)
  - Присоединение к комнате по `room_id`
  - Проверка баланса игрока
  - Проверка надежности для `reliable_only` комнат
  - Добавление игрока в комнату
  - Уведомление других игроков через Centrifugo

- [x] **Эндпоинт `/friends/{tg_id}`** (`app/friends/router.py`)
  - Возвращает список друзей с их именами
  - Формат: `{"tg_id": ..., "friends": [{"tg_id": ..., "name": ..., "username": ...}]}`

- [x] **Конфигурация `FRONT_URL`** (`app/config.py`)
  - Настройка базового URL фронтенда
  - Используется для генерации ссылок на Mini App

- [x] **Интеграция с Telegram Bot** (`app/bot/create_bot.py`)
  - Отправка сообщений с кнопками `WebAppInfo`
  - Использование `InlineKeyboardMarkup` и `InlineKeyboardButton`

### База данных

- [x] **Модель `Friend`** (`app/friends/models.py`)
  - Хранение связей дружбы
  - Правильные типы данных (`Integer` для `user_id` и `friend_id`)

- [x] **Модель `User`** (`app/users/models.py`)
  - Хранение информации о пользователях
  - Связь с друзьями через `Friend`

### Логика надежности

- [x] **Проверка надежности игроков** (`app/game/api/reliability.py`)
  - Функция `check_player_reliability`
  - Проверка последних 10 игр на ливы

- [x] **Интеграция в `/find_player`** (`app/game/api/router.py`)
  - Проверка надежности при создании `reliable_only` комнат
  - Проверка надежности при присоединении к `reliable_only` комнатам

- [x] **Интеграция в `/join_room`** (`app/game/api/router.py`)
  - Проверка надежности при присоединении по приглашению

### Redis и состояние

- [x] **Хранение комнат в Redis** (`app/game/redis_dao/`)
  - Создание и обновление комнат
  - Получение состояния комнаты

- [x] **Centrifugo для уведомлений** (`app/game/api/utils.py`)
  - Отправка событий в реальном времени
  - Каналы `room#{room_id}`, `user#{tg_id}`, `rooms`

## ⚠️ Требуется реализация на фронтенде

### Обработка параметра `room` в URL

- [ ] **Чтение параметра из URL**
  ```javascript
  const urlParams = new URLSearchParams(window.location.search);
  const roomIdFromUrl = urlParams.get('room');
  ```

- [ ] **Обработка приглашения при загрузке**
  - Проверка параметра `room` в `useEffect`
  - Приоритет обработки приглашения над проверкой существующих комнат
  - Вызов `handleJoinRoom(roomIdFromUrl)`

- [ ] **Очистка URL после обработки**
  ```javascript
  window.history.replaceState({}, '', window.location.pathname);
  ```

### UI для приглашения

- [ ] **Кнопка "Пригласить друга"**
  - В интерфейсе комнаты (когда игра еще не началась)
  - Открытие модального окна со списком друзей

- [ ] **Список друзей**
  - Загрузка через `GET /friends/{tg_id}`
  - Отображение имен друзей
  - Кнопка приглашения для каждого друга

- [ ] **Отправка приглашения**
  - Вызов `POST /friends/invite_to_room`
  - Обработка успешного ответа
  - Обработка ошибок (недостаточно средств, комната заполнена, и т.д.)

### Обработка ошибок

- [ ] **Ошибки при присоединении по приглашению**
  - "Комната не найдена"
  - "Комната уже заполнена"
  - "Недостаточно средств"
  - "Игра уже началась"
  - "Эта комната только для надежных игроков"

- [ ] **Ошибки при отправке приглашения**
  - "Пользователь не в списке друзей"
  - "Комната уже заполнена"
  - "У друга недостаточно средств"

## 📋 Тестирование

### Сценарии для проверки

1. **Успешное приглашение:**
   - [ ] Игрок А создает комнату
   - [ ] Игрок А приглашает друга Б
   - [ ] Друг Б получает сообщение от бота
   - [ ] Друг Б нажимает кнопку в сообщении
   - [ ] Mini App открывается с параметром `?room={room_id}`
   - [ ] Друг Б автоматически присоединяется к комнате

2. **Комната заполнена:**
   - [ ] Игрок А приглашает друга Б
   - [ ] Тем временем в комнату присоединяется другой игрок
   - [ ] Друг Б переходит по ссылке
   - [ ] Показывается ошибка "Комната уже заполнена"

3. **Недостаточный баланс:**
   - [ ] Игрок А приглашает друга Б
   - [ ] У друга Б недостаточно средств
   - [ ] При отправке приглашения возвращается ошибка
   - [ ] Игрок А видит сообщение об ошибке

4. **reliable_only комната:**
   - [ ] Игрок А создает `reliable_only` комнату
   - [ ] Игрок А приглашает ненадежного друга Б
   - [ ] При присоединении возвращается ошибка о надежности
   - [ ] Друг Б не может присоединиться

5. **Повторное использование ссылки:**
   - [ ] Друг Б успешно присоединился по ссылке
   - [ ] Б закрыл Mini App
   - [ ] Б снова открывает Mini App по той же ссылке
   - [ ] Система восстанавливает игру или показывает "Уже в комнате"

## 🔧 Конфигурация

### Переменные окружения

- [ ] `FRONT_URL` — базовый URL фронтенда
  - Пример: `https://grantexpert.pro/frontend`
  - Используется для генерации ссылок на Mini App

### Настройки бота

- [ ] `BOT_TOKEN` — токен Telegram бота
  - Используется для отправки сообщений с приглашениями

## 📝 Документация

- [x] **Многопользовательность и режимы игры** (`docs/multiplayer_and_game_modes.md`)
  - Описание всех режимов игры
  - Система надежности
  - Финансовая система

- [x] **Концепция работы ссылки на игру** (`docs/invite_link_concept.md`)
  - Полный поток работы приглашения
  - Сценарии использования
  - Обработка ошибок

- [x] **Чеклист готовности** (`docs/invite_implementation_checklist.md`)
  - Текущий файл

## ✨ Итог

### Готово на бэкенде: ✅
- Все эндпоинты реализованы
- Валидация и проверки работают
- Интеграция с Telegram Bot настроена
- Redis и Centrifugo интегрированы

### Требуется на фронтенде: ⚠️
- Обработка параметра `room` в URL
- UI для приглашения друзей
- Обработка ошибок при присоединении

### Рекомендации:
1. Реализовать обработку параметра `room` в `useEffect` при загрузке
2. Добавить кнопку "Пригласить друга" в интерфейс комнаты
3. Создать модальное окно со списком друзей
4. Добавить обработку всех ошибок с понятными сообщениями
5. Протестировать все сценарии из списка выше

